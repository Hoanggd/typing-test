import React from "react";
import styled from "styled-components/macro";

const Register = (props) => {
  const { className } = props;
  return (
    <div className={className}>Register</div>
    );
};

export default Register;
